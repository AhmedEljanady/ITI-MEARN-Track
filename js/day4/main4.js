let par = document.querySelector("#PAR");

const ChangeFont = function (str) {
  par.style.fontFamily = `${str}`;
};
const ChangeAlign = function (str) {
  par.style.textAlign = `${str}`;
};
const ChangeHeight = function (str) {
  par.style.height = `${str}`;
};
const ChangeLSpace = function (str) {
  par.style.letterSpacing = `${str}`;
};
const ChangeIndent = function (str) {
  par.style.textIndent = `${str}`;
};
const ChangeTransform = function (str) {
  par.style.textTransform = `${str}`;
};
const ChangeDecorate = function (str) {
  par.style.textDecoration = `${str}`;
};
const ChangeBorder = function (str) {
  par.style.borderStyle = `${str}`;
};
const ChangeBorderColor = function (str) {
  par.style.borderColor = `${str}`;
};
